apiVersion: v1
kind: Service
metadata:
  name: mysql-svc
  namespace: episode5
  labels:
    app: mysql-db
spec:
  selector:
    app: mysql-db
  ports:
    - name: mysql-db
      port: 3306
      targetPort: 3306
  clusterIP: None